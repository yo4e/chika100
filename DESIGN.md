# 『地下百階まで、配達です。』設計書

Status: Initial design revised  
Date: 2026-08-02  
Repository: `yo4e/chika100`

## 1. プロジェクト概要

『地下百階まで、配達です。』は、深夜の建物を地下100階まで降り、正体不明の受取人へ荷物を届ける、ブラウザ向けの短編ターン制ローグライクである。

本作はゲーム制作そのものに加え、RenderのWeb Serviceを実際に使い、Cloudflare中心の静的配信・サーバーレス構成との違いを体験的に理解するための実験プロジェクトでもある。

最初の公開範囲は、家族がURLを開いて遊べる状態とする。その後、一般公開しても破綻しにくい構造を選ぶ。ただし、初版から商用運営や大規模アクセスを想定しすぎない。

## 2. 2026-08-02の確定判断

- Render Postgresは使用しない
- サーバー側ランキングは実装しない
- プレイヤー名その他の個人情報を収集しない
- スコアは各プレイ終了時に端末上で計算・表示する
- 端末内の自己ベスト保存は `localStorage` を使ってよい
- 家族間では画面表示またはスクリーンショットでスコアを見比べる
- Renderは、状態を永続保存しないNode.js Web Serviceとして利用する

Postgresの30日制限をこの実験で消費せず、将来、永続データを本当に必要とする別プロジェクトのために温存する。

## 3. 目的と優先順位

1. RenderのWeb Serviceを使い、普通のNode.jsサーバーを公開する体験を得る
2. Codexが一日程度の集中実装で、開始からクリアまたはゲームオーバーまで遊べる作品にする
3. Macとスマートフォンの双方から、家族が説明なしでも遊べるようにする
4. 一般公開へ拡張できる最低限の安全性と保守性を持たせる
5. Cloudflareで静的サイトやWorkersを使う場合との設計差を理解する
6. ゲームとして、もう一度遊びたくなる小さな変化と選択を作る

本作のゲーム部分だけを考えれば、静的ホスティングでも成立する。今回はRenderの学習が主目的なので、Web Serviceに最低限だが実質的な責務を持たせる。

## 4. Renderを使う意味

Render上のNode.js Web Serviceは、次を担当する。

- ゲーム画面と静的アセットの配信
- 日本時間の日付を基準にしたデイリーシードの発行
- 公開可能なアプリ設定、バージョン、ビルド識別子の返却
- health checkへの応答
- 起動、APIアクセス、エラーのログ出力

サーバーは状態を保存しない。ゲームの進行、スコア、自己ベストはブラウザ側で管理する。

これにより、次を実際に観察できる。

- Node.jsプロセスのbuildとstart
- `PORT`へのbind
- GitHub連携と自動デプロイ
- `render.yaml`による構成管理
- health check
- runtime logs
- 環境変数
- 無料Web Serviceのスピンダウンと復帰
- 静的サイトだけを置く場合との違い

後にCloudflare Workersへ移植する場合、同じデイリーシードAPIをリクエスト単位の実行へ置き換えられる。この比較自体を実験成果とする。

## 5. Codexへの実装方針

- この設計書を要求仕様の基準とする
- 細部のアルゴリズム、ディレクトリ構成、ライブラリ選定、画面レイアウトはCodexが合理的に決定してよい
- ブロッキング要因がない限り、細かな選択を逐一確認せず、実装、テスト、修正まで自律的に進める
- 見た目だけの試作品ではなく、タイトル、ゲーム本編、結果、再挑戦までを一本につなぐ
- ローカルで動くだけで終わらせず、Renderへデプロイ可能な状態までを完成とする
- 一日の実装規模を超えそうな場合は、後述の「削る順序」に従う
- 秘密情報や認証情報をリポジトリへコミットしない
- 不要なデータ送信や解析サービスを追加しない

## 6. 世界観とトーン

### タイトル

地下百階まで、配達です。

内部表記は `Delivery to B100` または `chika100` を使用してよい。画面上の主タイトルは日本語を優先する。

### 導入

深夜、最後の配達先として指定された古い建物に入る。

住所には部屋番号がなく、ただ「地下100階」とだけ書かれている。荷物用エレベーターは下へしか動かず、扉が開くたび、建物の構造が変わっている。

荷物を壊さず、地下100階の受取人まで届ける。

### トーン

- 少し不穏だが、ホラー一辺倒ではない
- 配達業務として淡々と処理する可笑しさを持つ
- 流血、残酷描写、強い驚かせ演出は使わない
- 子どもを含む家族が遊べる内容にする
- 最下層の受取人について説明しすぎず、余韻を残す

## 7. 想定プレイ

- 対象: ゲームに詳しくない人を含む家族、短時間のブラウザゲームを遊ぶ一般ユーザー
- 対応: PCブラウザ、スマートフォンブラウザ
- 1プレイ: 8〜15分程度
- 操作: キーボードと画面上のタッチ操作
- プレイ人数: 一人
- アカウント: 不要
- 通信: 初回表示、デイリーシード、公開設定の取得のみ
- オフライン耐性: API取得失敗時もローカルの代替シードで遊べること

## 8. ゲームの基本構造

### 8.1 地下100階の扱い

100枚のマップは作らない。プレイ対象は10区画とし、荷物用エレベーターが10階単位で停止する。

`B10 → B20 → B30 → … → B90 → B100`

各区画は短いランダムマップで、出口のエレベーターへ到達すると次へ進む。B100のみ最終区画とする。

### 8.2 基本ループ

1. 区画へ到着する
2. 1マスずつ移動する
3. 敵、障害物、回復物、荷物補修品を処理する
4. 出口のエレベーターへ到達する
5. 三択の配達スキルから一つ選ぶ
6. 次の10階へ降りる
7. B100で最終障害を越え、荷物を渡す

### 8.3 ターン制

- プレイヤーが1マス移動または行動すると、敵も1ターン行動する
- 敵へ移動しようとすると近接攻撃になる
- 斜め移動は初版では不要
- 操作待ちの間、ゲーム内時間は進まない
- キーリピートと連続タップによる誤操作を抑制する

### 8.4 プレイヤーの状態

最低限、次の二つを持つ。

- 体力: 0になるとゲームオーバー
- 荷物状態: 0になると配達失敗

敵の通常攻撃は主に体力を減らす。罠、転倒、特定の敵、無理な行動は荷物状態を減らす。「敵を倒す」だけでなく「荷物を守って届ける」を中心にする。

### 8.5 マップ

推奨規模は1区画あたり約15〜21列 × 11〜15行。

必須条件:

- 開始地点から出口へ必ず到達可能
- スマートフォン画面に全体または十分な範囲が収まる
- 生成失敗時は再生成し、無限ループしない
- 同一シードでは同一のマップと配置を再現できる
- 行き止まりにはアイテム、近道、危険など何らかの意味を持たせる

生成方式はCodexが安定して実装・テストできるものを選んでよい。

## 9. 初版のコンテンツ上限

### 敵

通常敵は4種類まで。

例:

- 清掃ロボ: 決まった方向を往復する
- 警備ドローン: 視線が通ると追跡する
- 影の住人: 不規則に近づく
- 暴走台車: 直線上を突進し、荷物にもダメージを与える

B100に最終障害を1種類置いてよい。受取人そのものを敵にする必要はない。

### アイテム

5種類程度。

- 栄養ドリンク: 体力回復
- ガムテープ: 荷物状態回復
- マスターキー: 扉または近道を一度開く
- 緩衝材: 次の荷物ダメージを無効化
- 台車: 数ターンだけ移動または攻撃を強化

### 配達スキル

全体で8種類程度。各区画終了時に三択で一つ獲得する。

例:

- 安全靴: 罠ダメージ軽減
- 補強梱包: 荷物状態の最大値増加
- 夜間視力: 視界拡大
- 配達勘: 出口方向のヒント表示
- 護身術: 攻撃力増加
- 予備伝票: 一度だけ致命的な配達失敗を防ぐ

### 地形・罠

3種類程度。

- 滑る床
- 開閉する防火扉
- 荷物へダメージを与える段差

## 10. 難易度

- 初見でもB30前後までは進みやすくする
- 何度か遊べばB100へ到達できる難易度にする
- 敵の数と強さは階層に応じて段階的に上げる
- ランダム生成だけで理不尽な即死を起こさない
- 長文チュートリアルを避け、最初の数手で移動、攻撃、荷物状態を理解できるようにする
- 「簡単すぎる」より「何が起きたか分からず死ぬ」を優先して避ける

## 11. デイリー配達とリプレイ性

### デイリー配達

Render上のサーバーが、日本時間の日付からデイリーシードを返す。同じ日には全員が同じ建物へ挑戦する。

表示例:

`本日の建物: 2026-08-02便`

APIが利用できない場合、クライアントが同じ規則で代替シードを生成し、ゲーム本編は続行できるようにする。

### 練習配達

ランダムシードで遊べるモード。初版で時間が足りない場合は後回しにしてよい。

## 12. スコア

スコアはブラウザ内で計算し、結果画面に内訳を表示する。

要素:

- 配達成功または到達階
- 荷物の残存状態
- 残り体力
- 使用ターン数

推奨:

- 到達階による基礎点
- 配達成功ボーナス
- 荷物状態ボーナス
- 残り体力ボーナス
- 過剰なターンへの小さな減点

速度だけが正義にならず、荷物を守るプレイにも価値を持たせる。

結果画面には次を表示する。

- 合計スコア
- 到達階
- 配達成否
- 荷物状態
- 残り体力
- 使用ターン数
- デイリー便名またはシード
- 自己ベストとの差

自己ベストは端末内に保存してよい。サーバーへスコアを送信しない。

## 13. 技術構成

### 必須構成

- 単一リポジトリ
- Node.jsベースのRender Web Service
- 同一Web Serviceがゲーム画面の静的ファイルとAPIを配信する
- HTTPサーバーは `process.env.PORT` を使用し、`0.0.0.0` にbindする
- GitHubの `main` ブランチから自動デプロイできる
- ルートに `render.yaml` を置き、Blueprintとして構成を再現できる
- `/api/health` をRenderのHTTP Health Checkに使う
- 本番設定は環境変数で切り替える
- Renderのログで起動とAPIエラーを確認できる
- データベース、永続ディスク、サーバー側セッションは使わない

### 推奨スタック

- TypeScript
- Node.js + Express、Fastifyなどの小さなHTTPフレームワーク
- フロントエンドはCanvas 2D、DOMグリッド、軽量ゲームライブラリのいずれでもよい
- npm
- テストはVitest、Node標準テスト、Supertest等

ゲームエンジンの採用は自由だが、ビルドを複雑化させず、スマートフォン対応とデプロイ成功を優先する。

## 14. API

### `GET /api/health`

- プロセスがリクエストを処理できることを返す
- 正常時は2xx
- 外部サービスへ依存しない

### `GET /api/config`

公開してよいクライアント設定を返す。

例:

- アプリのバージョン
- ビルド識別子
- デイリーシード方式のバージョン
- サーバー時刻
- 実行環境名

### `GET /api/daily`

返却例:

- 日本時間の対象日
- その日の決定的なシード
- 建物名または便名
- シード方式のバージョン

同じ日、同じバージョンでは同じ値を返す。秘密値は不要。

### 不要なAPI

初版では次を作らない。

- スコア登録
- ランキング取得
- ユーザー登録
- ログイン
- セーブデータ同期
- 任意の自由入力を受け取るAPI

## 15. `render.yaml`

Codexは実装時点のRender公式仕様を確認したうえで、有効なBlueprintを作る。

想定内容:

- Node.js Web Service
- Singaporeリージョンを第一候補とする
- Freeプランで試せる場合はFreeを選ぶ
- build command
- start command
- `healthCheckPath: /api/health`
- GitHub pushによる自動デプロイ
- `NODE_ENV=production`

Postgresその他のデータストアは定義しない。

## 16. Renderで試す項目

1. GitHubリポジトリからWeb Serviceを作成する
2. `render.yaml`からBlueprintを同期する
3. build commandとstart commandが動く
4. `PORT`を使って外部公開できる
5. `onrender.com`のURLで家族がアクセスできる
6. `main`へのpushで自動デプロイされる
7. `/api/health`がデプロイ判定に使われる
8. Dashboardでbuild logsとruntime logsを確認する
9. 環境変数をコード外で設定する
10. アイドル後のスピンダウンと復帰時の待ち時間を体験する
11. 再起動や再デプロイでサーバー側状態が残らないことを確認する
12. 可能なら失敗したデプロイ、手動再デプロイ、ロールバックの挙動を見る

観察結果は `docs/render-notes.md` に追記する。仕様の説明だけでなく、実際に感じた待ち時間、画面、ログの見やすさ、設定の分かりやすさも記録する。

## 17. Cloudflareとの比較観点

Cloudflare版を同時に実装する必要はない。Render版を動かした後、次を比較する。

| 観点 | Renderで観察すること | Cloudflareで比較すること |
|---|---|---|
| 実行モデル | 常駐するNode.jsプロセス、起動、停止、再起動 | Workersのリクエスト単位・エッジ実行 |
| 配信 | 一つのWeb Serviceから静的ファイルとAPIを配信 | Static AssetsとWorkersの統合配信 |
| Node互換 | 一般的なNode.jsアプリとして動く範囲 | WorkersのNode.js互換と制約 |
| 状態 | プロセスメモリとエフェメラルFS。ただし本作では永続利用しない | KV、D1、Durable Objects等を必要時だけ選ぶ |
| デプロイ | Git push、build、プロセス起動、インスタンス切替 | WranglerまたはGit連携によるエッジ配布 |
| 待ち時間 | 無料サービスのスピンダウンと復帰 | リクエスト実行と地理的分散 |
| 可観測性 | Deploy logs、runtime logs、health checks | Workers Logs、Analytics等 |
| 構成の直感性 | 普通のサーバーに近いか | エッジ／サーバーレスとして考える必要があるか |

比較は「どちらが上か」を決めるためではなく、どの種類のアプリにどちらが自然かを理解するために行う。

## 18. 画面構成

### タイトル

- タイトル
- 短い導入文
- 「本日の配達を始める」
- 「練習配達」 任意
- 「遊び方」
- 本日の便名
- 自己ベスト

### ゲーム

- マップ
- 現在階
- 体力
- 荷物状態
- 所持アイテムまたは一時効果
- 簡潔な行動ログ
- PC用キー案内
- スマートフォン用方向ボタンと必要な行動ボタン

### 階間

- 到達階
- 配達スキル三択
- 次の階へ進む

### 結果

- 配達成功または失敗理由
- 到達階
- スコアと内訳
- 自己ベストとの差
- デイリー便名またはシード
- 再挑戦

## 19. UI・デザイン

### 視覚方針

- 深夜の業務用建物
- コンクリート、非常灯、荷物用エレベーター、階数表示
- 暗色を基調にしてよいが、文字とマップの判読性を最優先する
- 台車、ガムテープ、伝票、非常口など日常物をゲーム記号にする
- ピクセルアート、ASCII風、単純な図形のいずれでも成立させられる
- 外部画像素材がなくても完成する設計を優先する

### 操作性

- タップ対象はスマートフォンで押しやすい大きさにする
- 読み込み中と通信失敗を画面に明示する
- API障害時もゲーム本編は遊べるようにする
- 色だけで体力、危険、出口を区別しない
- 音声は任意。音を付ける場合はミュートを用意する

## 20. 一般公開を見据えた最低限の安全性

- ユーザー登録、名前、メールアドレス、位置情報を収集しない
- Cookieを必須にしない
- 自由入力欄を作らない
- APIは読み取り専用にする
- エラーレスポンスに環境変数、スタックトレース、内部パスを出さない
- 依存パッケージを増やしすぎない
- セキュリティヘッダーを適切に設定する
- アクセス解析や外部広告を初版へ入れない
- READMEに、自己ベストが端末内だけへ保存されることを明記する

## 21. テスト要件

### ゲームロジック

- 同一シードから同一結果が得られる
- 開始地点から出口へ到達可能
- 生成が規定回数以内で終わる
- 移動、攻撃、ダメージ、回復
- 体力または荷物状態が0になった際の終了
- 階層進行
- スコア計算
- 自己ベスト更新

### API

- health checkが2xxを返す
- daily seedの日付と決定性
- configが秘密情報を含まない
- API障害時にクライアントが代替シードへ移れる

### ビルド

- `npm test`
- `npm run build`
- `npm start`

上記がクリーンな環境で動くこと。

ブラウザE2Eは余力があれば追加する。タイトルから開始し、数手動かせるスモークテストがあると望ましい。

## 22. 完成条件

- READMEの手順でローカル起動できる
- タイトル画面からB100の成功または途中失敗まで通して遊べる
- 10区画の進行がある
- PCとスマートフォンの両方で操作できる
- デイリーシードが同日で一致する
- 結果画面にスコアと内訳が出る
- 端末内で自己ベストを保存できる
- Render Web Serviceとしてデプロイできる
- `onrender.com` URLを家族へ渡して遊んでもらえる
- health checkが設定されている
- GitHub pushから自動デプロイできる
- API障害時もゲーム本編を開始できる
- テストと本番ビルドが通る
- READMEにローカル起動、Renderデプロイ、環境変数、既知の制約が書かれている
- `docs/render-notes.md` に観察項目の雛形がある

## 23. 一日の実装順序

1. プロジェクト初期化、ローカルWeb Service、health check
2. シード付きマップ生成と移動
3. 敵、戦闘、体力、荷物状態
4. 10区画進行とB100の終了
5. タッチUIとタイトル・結果画面
6. スコアと自己ベスト
7. デイリーAPI、config API、フォールバック
8. `render.yaml` とRenderデプロイ
9. 自動テスト
10. 家族試遊を想定した難易度・操作修正
11. READMEとRender観察ノート

実装中も節目ごとに `npm test` と `npm run build` を通す。

## 24. 時間超過時に削る順序

上から順に削る。

1. 音、細かなアニメーション、装飾演出
2. 練習配達
3. 敵、アイテム、スキルの種類数
4. B100専用の複雑なボス挙動
5. ブラウザE2Eテスト
6. 詳細な自己ベスト表示

削ってはいけないもの:

- Render Web Serviceとしてのデプロイ
- health check
- GitHubからの自動デプロイ
- 開始から終了までのゲームループ
- 10区画でB100へ到達する構造
- スマートフォン操作
- 同一シードの決定性
- 結果画面のスコア
- API障害時もゲーム本編が遊べること

## 25. 初版後の候補

初版には含めない。

- 建物や受取人の追加シナリオ
- リプレイコードまたはシード共有
- 実績
- 複数の配達員
- 荷物の種類によるルール変化
- サーバー側ランキング
- 永続データベース
- WebSocketを使った観戦や同時配達
- Cloudflare Workers版の試作
- 独自ドメイン
- PWA化

## 26. 実験記録の結論として答えたい問い

- Renderで普通のNode.jsサーバーを公開するとは、実際にはどのような体験か
- GitHub pushから公開まで、何が自動で行われるか
- health check、ログ、環境変数はどこで設定し、どう見えるか
- 無料Web Serviceの休止と復帰は、家族が遊ぶゲームでどの程度気になるか
- 状態を保存しない小さなゲームにWeb Serviceを使うことは、自然だったか、過剰だったか
- 静的サイトだけをCloudflareへ置く場合と比べ、サーバーを持つ意味は何だったか
- 同じデイリーAPIをCloudflare Workersへ移す場合、どこを考え直す必要があるか
- 次の小規模アプリではRenderとCloudflareのどちらを自然に選ぶか

Renderを採用し続けること自体を成功条件にしない。ゲームを一本公開した経験から、両者の設計思想の違いを理解できた時点で実験として成功とする。

## 参考

実装時には最新版を確認すること。

- Render Web Services: https://render.com/docs/web-services
- Render Blueprints: https://render.com/docs/infrastructure-as-code
- Render Blueprint YAML Reference: https://render.com/docs/blueprint-spec
- Render Health Checks: https://render.com/docs/health-checks
- Render Deploys: https://render.com/docs/deploys
- Render Free Instances: https://render.com/docs/free
- Cloudflare Workers: https://developers.cloudflare.com/workers/
- Cloudflare Workers Static Assets: https://developers.cloudflare.com/workers/static-assets/
- Cloudflare Workers Node.js Compatibility: https://developers.cloudflare.com/workers/runtime-apis/nodejs/
