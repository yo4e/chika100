# 『地下百階まで、配達です。』設計書

Status: Initial design  
Date: 2026-08-02  
Repository: `yo4e/chika100`

## 1. プロジェクト概要

『地下百階まで、配達です。』は、深夜の建物を地下100階まで降り、正体不明の受取人へ荷物を届ける、ブラウザ向けの短編ターン制ローグライクである。

本作はゲーム制作そのものに加え、Renderを実際に使い、Cloudflare中心の構成との違いを体験的に理解するための実験プロジェクトでもある。

最初の公開範囲は家族がURLを開いて遊べる状態とする。その後、一般公開しても破綻しにくい構造を最初から選ぶ。ただし、初版から商用運営や大規模アクセスを想定しすぎない。

## 2. 目的と優先順位

優先順位は次のとおり。

1. RenderのWeb Serviceを使った、サーバーを持つWebアプリの公開を一通り体験する
2. Codexが一日程度の集中実装で、開始からクリアまたはゲームオーバーまで遊べる作品にする
3. Mac、スマートフォンの双方から、家族が説明なしでも遊べるようにする
4. 一般公開へ拡張できる最低限の安全性と保守性を持たせる
5. ゲームとして、もう一度遊びたくなる小さな変化と選択を作る

本作の主目的はRenderの学習である。静的サイトだけで完結させず、Render上でNode.jsのWeb ServiceとAPIを動かす意味がある構成にする。

## 3. Codexへの実装方針

- この設計書を要求仕様の基準とする
- 細部のアルゴリズム、ディレクトリ構成、ライブラリ選定、画面レイアウトはCodexが合理的に決定してよい
- ブロッキング要因がない限り、細かな選択を逐一確認せず、実装・テスト・修正まで自律的に進める
- 一日の実装規模を超えそうな場合は、後述の「削る順序」に従う
- 見た目だけの試作品ではなく、タイトル画面、ゲーム本編、結果、再挑戦までを一本につなぐ
- ローカルで動くだけで終わらせず、Renderへデプロイ可能な状態までを完成とする
- 秘密情報、認証情報、接続文字列をリポジトリへコミットしない

## 4. 世界観とトーン

### タイトル

地下百階まで、配達です。

英語表記または内部表記が必要な場合は `Delivery to B100` または `chika100` を使用してよい。画面上の主タイトルは日本語を優先する。

### 導入

深夜、最後の配達先として指定された古い建物に入る。

住所には部屋番号がなく、ただ「地下100階」とだけ書かれている。荷物用エレベーターは下へしか動かず、扉が開くたび、建物の構造が変わっている。

荷物を壊さず、地下100階の受取人まで届ける。

### トーン

- 少し不穏だが、ホラー一辺倒ではない
- 配達業務として淡々と処理する可笑しさを持つ
- 流血、残酷描写、強い驚かせ演出は使わない
- 子どもを含む家族が遊べる内容にする
- 最下層の受取人について、説明しすぎない余韻を残す

## 5. 想定プレイ

- 対象: ゲームに詳しくない人を含む家族、短時間のブラウザゲームを遊ぶ一般ユーザー
- 対応: PCブラウザ、スマートフォンブラウザ
- 1プレイ: 8〜15分程度
- 操作: キーボードと画面上のタッチ操作
- プレイ人数: 一人
- 通信: 開始時のデイリーシード取得、結果送信、ランキング取得のみ
- アカウント: 不要

## 6. ゲームの基本構造

### 6.1 地下100階の扱い

100枚のマップは作らない。

プレイ対象は10区画とし、荷物用エレベーターが10階単位で停止する。

`B10 → B20 → B30 → … → B90 → B100`

各区画は短いランダムマップで、出口のエレベーターへ到達すると次へ進む。B100のみ最終区画とする。

### 6.2 基本ループ

1. 区画へ到着する
2. 周囲を確認し、1マスずつ移動する
3. 敵、障害物、回復物、荷物補修品を処理する
4. 出口のエレベーターへ到達する
5. 三択の配達スキルから一つ選ぶ
6. 次の10階へ降りる
7. B100で最終障害を越え、荷物を渡す

### 6.3 ターン制

- プレイヤーが1マス移動または行動すると、敵も1ターン行動する
- 敵へ移動しようとすると近接攻撃になる
- 斜め移動は初版では不要
- 操作待ちの間、ゲーム内時間は進まない
- 誤操作を減らすため、キーリピートと連続タップを適切に抑制する

### 6.4 プレイヤーの状態

最低限、次の二つを持つ。

- 体力: 0になるとゲームオーバー
- 荷物状態: 0になると配達失敗

敵の通常攻撃は主に体力を減らす。罠、転倒、特定の敵、無理な行動は荷物状態を減らす。これにより「敵を倒す」だけでなく「荷物を守って届ける」がゲームの中心になる。

初期値やダメージ量は実装後の試遊で調整する。

### 6.5 マップ

推奨規模は1区画あたり約15〜21列 × 11〜15行。

必須条件:

- 開始地点から出口へ必ず到達可能
- 行き止まりには、アイテム、近道、危険など何らかの意味を持たせる
- スマートフォン画面に全体または十分な範囲が収まる
- 生成失敗時は再生成し、無限ループしない
- 同一シードでは同一のマップと配置を再現できる

生成方式は、部屋と通路、セルオートマトン、テンプレート結合など、Codexが安定して実装・テストできるものを選んでよい。

## 7. 初版のコンテンツ上限

一日の実装規模を守るため、初版は次の範囲を上限とする。

### 敵

通常敵は4種類まで。

例:

- 清掃ロボ: 決まった方向を往復する
- 警備ドローン: 視線が通ると追跡する
- 影の住人: 不規則に近づく
- 暴走台車: 直線上を突進し、荷物にもダメージを与える

B100に最終障害を1種類置いてよい。受取人そのものを敵にする必要はない。

### アイテム

5種類程度。

例:

- 栄養ドリンク: 体力回復
- ガムテープ: 荷物状態回復
- マスターキー: 扉または近道を一度開く
- 緩衝材: 次の荷物ダメージを無効化
- 台車: 数ターンだけ移動または攻撃を強化

### 配達スキル

各区画終了時に三択で一つ獲得する。全体で8種類程度を用意し、重複可否は実装しやすい方式でよい。

例:

- 安全靴: 罠ダメージ軽減
- 補強梱包: 荷物状態の最大値増加
- 夜間視力: 視界拡大
- 配達勘: 出口方向のヒント表示
- 護身術: 攻撃力増加
- 予備伝票: 一度だけ致命的な配達失敗を防ぐ

### 地形・罠

3種類程度。

例:

- 滑る床
- 開閉する防火扉
- 荷物へダメージを与える段差

## 8. 難易度

- 初見でもB30前後までは進みやすくする
- 何度か遊べばB100へ到達できる難易度にする
- 敵の数と強さは階層に応じて段階的に上げる
- ランダム生成だけで理不尽な即死を起こさない
- チュートリアルを長文にせず、最初の数手で移動、攻撃、荷物状態を理解できるようにする
- 家族試遊の段階では「簡単すぎる」より「何が起きたか分からず死ぬ」を優先して避ける

## 9. デイリー配達とリプレイ性

### デイリー配達

サーバーが日本時間の日付からデイリーシードを返す。同じ日には全員が同じ建物へ挑戦する。

表示例:

`本日の建物: 2026-08-02便`

### 練習配達

任意シードまたはランダムシードで遊べるモード。ランキング対象外でもよい。

初版で時間が足りない場合、デイリー配達のみを必須とし、練習配達は後回しにする。

## 10. スコアとランキング

### スコア要素

- 配達成功または到達階
- 荷物の残存状態
- 残り体力
- 使用ターン数

厳密な式はCodexが実装時に決定してよいが、プレイヤーへ結果の内訳を表示する。

推奨例:

- 到達階による基礎点
- 配達成功ボーナス
- 荷物状態ボーナス
- 残り体力ボーナス
- 過剰なターンへの小さな減点

速度だけが正義にならず、荷物を守るプレイにも価値を持たせる。

### ランキング

- デイリー上位20件程度
- 名前は1〜12文字
- アカウント登録なし
- 同じ名前の重複を許可する
- 表示項目は名前、スコア、到達階、配達成否、登録時刻程度
- 自由記述は名前だけに限定する
- 制御文字、HTML、極端に長い文字列を拒否または除去する
- 初版は本格的な不正対策を行わない。「家族と小規模公開向けの名誉ランキング」と明記してよい

## 11. 技術構成

細部はCodexに委ねるが、Renderとの差を体験するため、次の構成原則を守る。

### 必須構成

- 単一リポジトリ
- Node.jsベースのRender Web Service
- 同一Web Serviceが、ゲーム画面の静的ファイルとAPIを配信する
- HTTPサーバーは `process.env.PORT` を使用し、`0.0.0.0` にbindする
- GitHubの`main`ブランチから自動デプロイできる
- ルートに `render.yaml` を置き、Blueprintとして構成を再現できる
- `/api/health` をRenderのHTTP Health Checkに使う
- 本番設定は環境変数で切り替える
- Renderのログで起動、APIエラー、DB接続状態を確認できる

### 推奨スタック

- TypeScript
- Node.js + Express、Fastifyなどの小さなHTTPフレームワーク
- フロントエンドはCanvas 2D、DOMグリッド、軽量ゲームライブラリのいずれでもよい
- npm
- テストはVitest、Node標準テスト、Supertest等

ゲームエンジンの採用は自由だが、ビルドを複雑化させず、スマートフォン対応とデプロイ成功を優先する。

### 静的ファイルとAPI

初版はフロントエンドとAPIを別サービスへ分割しない。同一オリジンにすることで、CORS設定を不要にし、Renderの「常駐するWebアプリを一つデプロイする」体験を明確にする。

## 12. API案

命名は変更してよいが、最低限の責務は維持する。

### `GET /api/health`

- プロセスがリクエストを処理できることを返す
- DBを使う構成では、軽量な接続確認を含めてもよい
- 正常時は2xx

### `GET /api/config`

公開してよいクライアント設定を返す。

例:

- アプリのバージョン
- ランキングの有効・無効
- デイリーの日付
- ビルド識別子

### `GET /api/daily`

- 日本時間の対象日
- その日の決定的なシード
- 必要なら建物名や便名

シード生成用の秘密値をクライアントへ返す必要はない。日付だけから生成してもよい。

### `GET /api/scores?date=YYYY-MM-DD`

デイリーランキングを返す。

### `POST /api/scores`

結果を登録する。

最低限のサーバー側検証:

- 型
- 文字数
- 数値範囲
- 日付
- 到達階が10刻みまたは許可された値であること
- 極端に不自然なスコアの拒否
- リクエストサイズ制限
- 簡単なレート制限

完全なチート防止は初版の対象外。

## 13. データ保存

### Render Postgres

Render Postgresを使い、ランキングがWeb Serviceの再起動や再デプロイ後も残ることを確認する。

最低限のテーブル例:

`score_entries`

- `id`
- `daily_date`
- `player_name`
- `score`
- `reached_floor`
- `delivered`
- `package_condition`
- `remaining_hp`
- `turns`
- `seed_version`
- `created_at`

インデックスは `daily_date` と `score` を中心に付ける。

### フォールバック

`DATABASE_URL` がないローカル環境でも、ゲーム本編は起動・プレイできるようにする。この場合、ランキングAPIは明示的に無効化するか、開発用メモリ保存へ落としてよい。

SQLiteを本番の永続保存として使わない。RenderのWeb Serviceのローカルファイルシステムは永続保存先として扱わない。

### マイグレーション

- 初回デプロイ時にテーブルを作成できる
- 再実行しても壊れない
- Renderの `preDeployCommand` または起動時の安全なマイグレーションを利用する
- 手順をREADMEに記載する

## 14. `render.yaml` に含めるもの

Codexは、設計時点のRender公式仕様を確認したうえで有効なBlueprintを作る。

想定内容:

- Node.jsのWeb Service
- Singaporeリージョンを第一候補とする
- Freeプランで試せる場合はFreeを選ぶ
- build command
- start command
- `healthCheckPath: /api/health`
- `autoDeployTrigger: commit`
- `NODE_ENV=production`
- Postgresのconnection stringを `DATABASE_URL` として参照
- 必要ならPostgresリソース

FreeプランやBlueprint仕様は変更されうるため、実装時点の公式ドキュメントを優先する。

## 15. Renderで試す項目

このプロジェクトでは、次を意図的に確認する。

1. GitHubリポジトリからWeb Serviceを作成する
2. `render.yaml` からBlueprintを同期する
3. build commandとstart commandが動く
4. `PORT` を使って外部公開できる
5. `onrender.com` のURLで家族がアクセスできる
6. `main`へのpushで自動デプロイされる
7. `/api/health` がデプロイ判定に使われる
8. Render Dashboardでビルドログと実行ログを確認する
9. 環境変数と秘密値をコード外で設定する
10. Web Serviceが休止・再起動・再デプロイした後も、Postgresのランキングが残る
11. 無料Web Serviceを使う場合、アイドル後のスピンダウンと再起動時の待ち時間を体験する
12. 可能なら、失敗したデプロイ、手動再デプロイ、ロールバックの挙動を見る

観察結果は `docs/render-notes.md` に追記する。仕様の説明だけでなく、実際に感じた待ち時間、画面、ログの見やすさ、設定の分かりやすさも記録する。

## 16. Cloudflareとの比較観点

Cloudflare版を同時に実装する必要はない。Render版を動かした後、次を比較する。

| 観点 | Renderで観察すること | Cloudflareで比較すること |
|---|---|---|
| 実行モデル | Node.jsプロセス、起動、停止、再起動 | Workersのリクエスト単位・エッジ実行 |
| 配信 | 一つのWeb Serviceから静的ファイルとAPIを配信 | Static AssetsとWorkersの統合配信 |
| Node互換 | 一般的なNode.jsアプリとして動く範囲 | WorkersのNode.js互換と制約 |
| 状態 | プロセスメモリ、Postgres、エフェメラルFS | D1、KV、Durable Objects等 |
| デプロイ | Git push、ビルド、インスタンス切替 | WranglerまたはGit連携によるエッジ配布 |
| 待ち時間 | 無料サービスのスピンダウンと復帰 | Workersの起動・地理的分散 |
| 可観測性 | Deploy logs、runtime logs、health checks | Workers Logs、Analytics等 |
| 構成の直感性 | 普通のサーバーに近いか | エッジ／サーバーレスとして考える必要があるか |

比較は「どちらが上か」を決めるためではなく、どの種類のアプリにどちらが自然かを理解するために行う。

## 17. 画面構成

初版は4画面相当でよい。SPAでも画面遷移でもよい。

### タイトル

- タイトル
- 短い導入文
- 「本日の配達を始める」
- 「遊び方」
- デイリーランキング
- 初回アクセス時に名前を設定

### ゲーム

- マップ
- 現在階
- 体力
- 荷物状態
- 所持アイテムまたは一時効果
- 簡潔な行動ログ
- PC用キー案内
- スマートフォン用方向ボタンと必要な行動ボタン

### 階間

- 到達階
- 配達スキル三択
- 次の階へ進む

### 結果

- 配達成功または失敗理由
- 到達階
- スコア内訳
- ランキング登録結果
- 再挑戦
- ランキング

## 18. UI・デザイン

### 視覚方針

- 深夜の業務用建物
- コンクリート、非常灯、荷物用エレベーター、階数表示
- 暗色を基調にしてよいが、文字とマップの判読性を最優先する
- 派手なファンタジー装備より、台車、ガムテープ、伝票、非常口など日常物をゲーム記号にする
- ピクセルアート、ASCII風、単純な図形のいずれでも成立させられる
- 外部画像素材がなくても完成する設計を優先する

### 操作性

- タップ対象はスマートフォンで押しやすい大きさにする
- 読み込み中、通信失敗、ランキング無効を画面に明示する
- API障害時もゲーム本編は遊べるようにする
- 色だけで体力、危険、出口を区別しない
- 音声は任意。ミュートが必要な音を必須にしない

## 19. 一般公開を見据えた最低限の安全性

- ユーザー登録、メールアドレス、位置情報を収集しない
- Cookieを必須にしない
- 名前以外の自由入力を作らない
- 名前はサーバー側でも検証し、出力時に安全に扱う
- JSON body sizeを小さく制限する
- スコア登録APIに軽量なレート制限を入れる
- エラーレスポンスに接続文字列、スタックトレース、内部パスを出さない
- 依存パッケージを増やしすぎない
- セキュリティヘッダーを適切に設定する
- READMEに「ランキングは実験的で、不正対策は限定的」と記載する

## 20. テスト要件

最低限、次を自動テストする。

### ゲームロジック

- 同一シードから同一結果が得られる
- 開始地点から出口へ到達可能
- 生成が規定回数以内で終わる
- 移動、攻撃、ダメージ、回復
- 体力または荷物状態が0になった際の終了
- 階層進行
- スコア計算

### API

- health checkが2xxを返す
- daily seedの日付と決定性
- 正常なスコア登録
- 不正な名前、数値、日付、巨大bodyの拒否
- ランキングの並び順と件数上限
- DBなし環境の挙動

### ビルド

- `npm test`
- `npm run build`
- `npm start`

上記がクリーンな環境で動くこと。

ブラウザE2Eは余力があれば追加する。タイトルから開始し、数手動かせるスモークテストがあると望ましい。

## 21. 完成条件

次を満たした時点を初版完成とする。

- リポジトリをcloneし、READMEの手順でローカル起動できる
- タイトル画面からB100の成功または途中失敗まで通して遊べる
- 10区画の進行がある
- PCとスマートフォンの両方で操作できる
- デイリーシードが同日で一致する
- Render Web Serviceとしてデプロイできる
- `onrender.com` URLを家族へ渡して遊んでもらえる
- health checkが設定されている
- GitHub pushから自動デプロイできる
- Render Postgres利用時、ランキングが再デプロイ後も残る
- テストと本番ビルドが通る
- READMEにローカル起動、Renderデプロイ、環境変数、DB、既知の制約が書かれている
- `docs/render-notes.md` に観察項目の雛形がある

## 22. 一日の実装順序

推奨順序:

1. プロジェクト初期化、ローカルWeb Service、health check
2. シード付きマップ生成と移動
3. 敵、戦闘、体力、荷物状態
4. 10区画進行とB100の終了
5. タッチUIとタイトル・結果画面
6. デイリーAPI
7. スコア、Postgres、ランキング
8. `render.yaml` とRenderデプロイ
9. 自動テスト
10. 家族試遊を想定した難易度・操作修正
11. READMEとRender観察ノート

実装中も、節目ごとに `npm test` と `npm run build` を通す。

## 23. 時間超過時に削る順序

上から順に削る。

1. 音、細かなアニメーション、装飾演出
2. 練習配達
3. 敵、アイテム、スキルの種類数
4. B100専用の複雑なボス挙動
5. 全期間ランキング
6. ブラウザE2Eテスト
7. 詳細なスコア不正検出

削ってはいけないもの:

- Render Web Serviceとしてのデプロイ
- health check
- GitHubからの自動デプロイ
- 開始から終了までのゲームループ
- 10区画でB100へ到達する構造
- スマートフォン操作
- 同一シードの決定性
- API障害時もゲーム本編が遊べること
- 最低限の入力検証

Postgresの接続で長時間詰まった場合も、ゲーム本編とRender Web Serviceの公開を先に成立させる。DBなしモードを維持し、Postgres統合を次のコミットへ切り離せる設計にする。

## 24. 初版後の候補

初版には含めない。

- 建物や受取人の追加シナリオ
- 週間ランキング
- リプレイコードまたはシード共有
- 実績
- 複数の配達員
- 荷物の種類によるルール変化
- WebSocketを使った観戦や同時配達
- Cloudflare Workers版の試作
- 独自ドメイン
- PWA化

## 25. 実験記録の結論として答えたい問い

完成後、少なくとも次へ自分たちの言葉で答えられる状態を目指す。

- Renderで「普通のNode.jsサーバーを公開する」とは、実際にはどのような体験か
- GitHub pushから公開まで、何が自動で行われるか
- health check、ログ、環境変数、データベースはどこで設定し、どう見えるか
- 無料Web Serviceの休止と復帰は、家族が遊ぶゲームでどの程度気になるか
- 静的サイトだけをCloudflareへ置く場合と比べ、サーバーを持つ意味は何だったか
- 同じゲームをCloudflare Workersへ移す場合、どこを考え直す必要があるか
- 次の小規模アプリでは、RenderとCloudflareのどちらを自然に選ぶか

このプロジェクトは、Renderを採用し続けること自体を成功条件にしない。ゲームを一本公開した経験から、両者の設計思想の違いを理解できた時点で、実験として成功とする。

## 参考

実装時には最新版を確認すること。

- Render Web Services: https://render.com/docs/web-services
- Render Blueprints: https://render.com/docs/infrastructure-as-code
- Render Blueprint YAML Reference: https://render.com/docs/blueprint-spec
- Render Health Checks: https://render.com/docs/health-checks
- Render Deploys: https://render.com/docs/deploys
- Render Free Instances: https://render.com/docs/free
- Cloudflare Workers: https://developers.cloudflare.com/workers/
- Cloudflare Workers Static Assets: https://developers.cloudflare.com/workers/static-assets/
- Cloudflare Workers Node.js Compatibility: https://developers.cloudflare.com/workers/runtime-apis/nodejs/
