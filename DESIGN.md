# 『地下百階まで、配達です。』設計書

Status: Initial implementation specification  
Updated: 2026-08-02  
Repository: `yo4e/chika100`

## 1. プロジェクト概要

『地下百階まで、配達です。』は、深夜の建物を地下100階まで降り、正体不明の受取人へ荷物を届ける、ブラウザ向けの短編ターン制ローグライクである。

本作はゲーム制作そのものに加え、RenderのWeb Serviceを実際に使い、Cloudflare中心の静的配信・サーバーレス構成との違いを体験的に理解するための実験プロジェクトでもある。

最初の公開範囲は、家族がURLを開いて遊べる状態とする。一般公開も視野に入れるが、初版から商用運営や大規模アクセスを想定しすぎない。

## 2. 現時点の確定判断

2026年8月2日時点では、次を確定事項とする。

- 初版ではRender Postgresを使用しない
- 初版ではサーバー側ランキングを実装しない
- プレイヤー名、メールアドレス、位置情報などの個人情報を収集しない
- スコアは各プレイ終了時にブラウザ上で計算・表示する
- 端末内の自己ベスト保存は `localStorage` を使用してよい
- 家族間では結果画面またはスクリーンショットでスコアを見比べる
- Renderは、状態を永続保存しないNode.js Web Serviceとして利用する
- 将来、ランキングや共有記録を本当に必要とした時点で、Render Postgresその他の永続保存を追加検討する

Postgresを不採用と決めたわけではない。初版では不要なので入れず、ゲームとRender公開を先に完成させる。

## 3. 今日の到達目標

2026年8月2日の作業では、次を最優先の完成条件とする。

1. GitHubリポジトリからRenderへデプロイできる
2. 発行されたURLを家族へ渡せる
3. Macとスマートフォンの双方から起動できる
4. タイトル画面からゲーム開始、途中失敗またはB100到達、結果表示、再挑戦まで通して遊べる
5. 結果画面に、到達階、配達成否、荷物状態、残り体力、使用ターン数、総合スコアを表示する
6. データベースやアカウントがなくても完全に遊べる
7. Renderのbuild、start、health check、ログ、自動デプロイを確認できる

今日の成功条件は「一般公開用の完成品」ではなく、「家族で実際に遊べる初版をRender上で動かすこと」である。

## 4. 目的と優先順位

1. RenderのWeb Serviceを使い、普通のNode.jsサーバーを公開する体験を得る
2. Codexが一日程度の集中実装で、開始から終了まで遊べる作品にする
3. 家族が説明なしでも遊べる操作性にする
4. Cloudflareで静的サイトやWorkersを使う場合との設計差を理解する
5. 一般公開へ拡張できる最低限の安全性と保守性を持たせる
6. 小品として、もう一度遊びたくなる変化と選択を作る

本作のゲーム部分だけを考えれば、静的ホスティングでも成立する。今回はRenderの学習が主目的なので、Web Serviceに最低限だが実質的な責務を持たせる。

## 5. Codexへの実装方針

- この設計書を要求仕様の基準とする
- 細部のアルゴリズム、ディレクトリ構成、ライブラリ選定、画面レイアウトはCodexが合理的に決定してよい
- ブロッキング要因がない限り、細かな選択を逐一確認せず、実装、テスト、修正まで自律的に進める
- 見た目だけの試作品ではなく、タイトル、ゲーム本編、結果、再挑戦までを一本につなぐ
- ローカルで動くだけで終わらせず、Renderへデプロイ可能な状態までを完成とする
- 一日の実装規模を超えそうな場合は、後述の「削る順序」に従う
- 秘密情報や認証情報をリポジトリへコミットしない
- 不要なデータ送信や解析サービスを追加しない
- 実装時点のRender公式仕様を確認し、古い設定例を盲目的に使わない

## 6. 世界観とトーン

### タイトル

地下百階まで、配達です。

内部表記は `Delivery to B100` または `chika100` を使用してよい。画面上の主タイトルは日本語を優先する。

### 導入

深夜、最後の配達先として指定された古い建物に入る。

住所には部屋番号がなく、ただ「地下100階」とだけ書かれている。荷物用エレベーターは下へしか動かず、扉が開くたび、建物の構造が変わっている。

荷物を壊さず、地下100階の受取人まで届ける。

### トーン

- 少し不穏だが、ホラー一辺倒ではない
- 配達業務として淡々と処理する可笑しさを持つ
- 流血、残酷描写、強い驚かせ演出は使わない
- 子どもを含む家族が遊べる内容にする
- 最下層の受取人について説明しすぎず、余韻を残す

## 7. 想定プレイ

- 対象: ゲームに詳しくない人を含む家族、短時間のブラウザゲームを遊ぶ一般ユーザー
- 対応: PCブラウザ、スマートフォンブラウザ
- 1プレイ: 8〜15分程度
- 操作: キーボードと画面上のタッチ操作
- プレイ人数: 一人
- アカウント: 不要
- 通信: 初回表示、デイリーシード、公開設定の取得のみ
- オフライン耐性: API取得失敗時もローカルの代替シードで遊べること

## 8. ゲームの基本構造

### 8.1 地下100階の扱い

100枚のマップは作らない。プレイ対象は10区画とし、荷物用エレベーターが10階単位で停止する。

`B10 → B20 → B30 → … → B90 → B100`

各区画は短いランダムマップで、出口のエレベーターへ到達すると次へ進む。B100のみ最終区画とする。

### 8.2 基本ループ

1. 区画へ到着する
2. 周囲を確認し、1マスずつ移動する
3. 敵、障害物、回復物、荷物補修品を処理する
4. 出口のエレベーターへ到達する
5. 三択の配達スキルから一つ選ぶ
6. 次の10階へ降りる
7. B100で最終障害を越え、荷物を渡す
8. 結果とスコアを表示する

### 8.3 ターン制

- プレイヤーが1マス移動または行動すると、敵も1ターン行動する
- 敵へ移動しようとすると近接攻撃になる
- 初版では上下左右の4方向移動とする
- 操作待ちの間、ゲーム内時間は進まない
- キーリピートと連続タップによる誤操作を適切に抑制する

### 8.4 プレイヤーの状態

最低限、次の二つを持つ。

- 体力: 0になるとゲームオーバー
- 荷物状態: 0になると配達失敗

敵の通常攻撃は主に体力を減らす。罠、転倒、特定の敵、無理な行動は荷物状態を減らす。「敵を倒す」だけでなく「荷物を守って届ける」を中心にする。

### 8.5 マップ

推奨規模は1区画あたり約15〜21列 × 11〜15行。

必須条件:

- 開始地点から出口へ必ず到達可能
- スマートフォン画面に全体または十分な範囲が収まる
- 生成失敗時は規定回数内で再生成し、無限ループしない
- 同一シードでは同一のマップと配置を再現できる
- ランダム生成だけで理不尽な即死を起こさない

生成方式は、Codexが最も安定して実装、検証できる方式を選んでよい。

## 9. 初版のコンテンツ上限

### 敵

通常敵は最大4種類。

例:

- 清掃ロボ: 決まった方向を往復する
- 警備ドローン: 視線が通ると追跡する
- 影の住人: 不規則に近づく
- 暴走台車: 直線上を突進し、荷物にもダメージを与える

B100には最終障害を1種類置いてよい。受取人そのものを敵にする必要はない。

### アイテム

最大5種類。

例:

- 栄養ドリンク: 体力回復
- ガムテープ: 荷物状態回復
- マスターキー: 扉または近道を一度開く
- 緩衝材: 次の荷物ダメージを無効化
- 台車: 数ターンだけ移動または攻撃を強化

### 配達スキル

全体で最大8種類。各区画終了時に三択から一つ選ぶ。

例:

- 安全靴: 罠ダメージ軽減
- 補強梱包: 荷物状態の最大値増加
- 夜間視力: 視界拡大
- 配達勘: 出口方向のヒント表示
- 護身術: 攻撃力増加
- 予備伝票: 一度だけ致命的な配達失敗を防ぐ

### 地形・罠

最大3種類。

例:

- 滑る床
- 開閉する防火扉
- 荷物へダメージを与える段差

## 10. 難易度

- 初見でもB30前後までは進みやすくする
- 数回遊べばB100へ到達できる難易度にする
- 階層に応じて敵の数と強さを段階的に上げる
- 長文チュートリアルを避け、最初の数手で移動、攻撃、荷物状態を理解できるようにする
- 家族試遊では「簡単すぎる」より「何が起きたか分からず死ぬ」を優先して避ける

## 11. デイリー配達とリプレイ性

### デイリー配達

Render上のAPIが、日本時間の日付から決定的なデイリーシードを返す。同じ日には全員が同じ建物へ挑戦できる。

表示例:

`本日の建物: 2026-08-02便`

デイリーシードAPIが利用できない場合、ブラウザ側で日付から代替シードを生成し、ゲームを続行できるようにする。

### 練習配達

任意またはランダムなシードで遊ぶモード。時間が足りなければ初版から削ってよい。

## 12. スコア

スコアはブラウザ側で計算し、結果画面に内訳を表示する。

評価要素:

- 配達成功または到達階
- 荷物の残存状態
- 残り体力
- 使用ターン数

速度だけが正義にならず、荷物を守るプレイにも価値を持たせる。

結果画面の必須表示:

- 配達成功または失敗理由
- 到達階
- 荷物状態
- 残り体力
- 使用ターン数
- 得点内訳
- 総合スコア
- 端末内自己ベストとの差
- 再挑戦ボタン

初版ではスコアをサーバーへ送信しない。

## 13. 技術構成

### 必須構成

- 単一リポジトリ
- Node.jsベースのRender Web Service
- 同一Web Serviceがゲーム画面の静的ファイルとAPIを配信する
- HTTPサーバーは `process.env.PORT` を使用し、`0.0.0.0` にbindする
- GitHubの `main` ブランチから自動デプロイできる
- ルートに `render.yaml` を置き、構成を再現できる
- `/api/health` をRenderのHTTP Health Checkに使う
- 本番設定は環境変数で切り替える
- Renderのログで起動、APIアクセス、エラーを確認できる
- データベース、永続ディスク、外部ストレージは初版では使用しない

### 推奨スタック

- TypeScript
- Node.js + Expressまたは同程度の小さなHTTPフレームワーク
- フロントエンドはCanvas 2D、DOMグリッド、軽量ゲームライブラリのいずれでもよい
- npm
- Vitest、Node標準テスト、Supertest等

ゲームエンジンの採用は自由だが、ビルドを複雑化せず、スマートフォン対応とデプロイ成功を優先する。

## 14. Renderを使う意味

Render上のNode.js Web Serviceは、次を担当する。

- ゲーム画面と静的アセットの配信
- 日本時間の日付を基準にしたデイリーシードの発行
- 公開可能なアプリ設定、バージョン、ビルド識別子の返却
- health checkへの応答
- 起動、APIアクセス、エラーのログ出力

サーバーは状態を保存しない。ゲームの進行、スコア、自己ベストはブラウザ側で管理する。

これにより、次を観察する。

- Node.jsプロセスのbuildとstart
- `PORT`へのbind
- GitHub連携と自動デプロイ
- `render.yaml`による構成管理
- health check
- runtime logs
- 環境変数
- 無料Web Serviceのスピンダウンと復帰
- 静的サイトだけを置く場合との違い

## 15. API

### `GET /api/health`

- プロセスがリクエストを処理できることを返す
- 正常時は2xx
- 重い処理や外部サービスへの依存を含めない

### `GET /api/config`

公開してよい設定を返す。

例:

- アプリのバージョン
- ビルド識別子
- デイリー機能の有効・無効
- サーバー時刻または対象日

### `GET /api/daily`

返却項目:

- 日本時間の対象日
- その日の決定的なシード
- 必要なら建物名または便名
- シード方式のバージョン

初版では書き込みAPIを作らない。

## 16. `render.yaml`

実装時点のRender公式仕様に合わせ、少なくとも次を含める。

- Node.js Web Service
- Freeプランを使用可能ならFreeを選ぶ
- build command
- start command
- `healthCheckPath: /api/health`
- `NODE_ENV=production`
- GitHub `main` へのpushによる自動デプロイ

リージョンは日本からの利用を考慮して選ぶ。名称や指定可能値は実装時点の公式仕様を確認する。

## 17. Renderで試す項目

1. Renderへ登録し、GitHubアカウントを接続する
2. `yo4e/chika100` をデプロイ対象として許可する
3. `render.yaml` またはDashboardからWeb Serviceを作成する
4. build commandとstart commandが動く
5. `PORT` を使って外部公開できる
6. `onrender.com` のURLで家族がアクセスできる
7. `main`へのpushで自動デプロイされる
8. `/api/health` がデプロイ判定に使われる
9. Dashboardでbuild logsとruntime logsを確認する
10. 環境変数をコード外で設定する
11. アイドル後のスピンダウンと次回アクセス時の復帰を体験する
12. 可能なら手動再デプロイ、失敗時のログ、ロールバックの導線を見る

観察結果は `docs/render-notes.md` に記録する。

## 18. Cloudflareとの比較観点

Cloudflare版を同時に実装する必要はない。Render版を動かした後、次を比較する。

| 観点 | Render | Cloudflareで比較すること |
|---|---|---|
| 実行モデル | 常駐するNode.jsプロセス、起動、停止、再起動 | Workersのリクエスト単位・エッジ実行 |
| 配信 | 一つのWeb Serviceから静的ファイルとAPIを配信 | Static AssetsとWorkersの統合配信 |
| Node互換 | 一般的なNode.jsアプリとして動く | WorkersのNode.js互換と制約 |
| 状態 | プロセスメモリとエフェメラルFS。初版では永続化なし | D1、KV、Durable Objects等 |
| デプロイ | Git push、build、インスタンス切替 | Git連携またはWranglerによる配布 |
| 待ち時間 | 無料サービスのスピンダウンと復帰 | エッジ実行の応答特性 |
| 可観測性 | deploy logs、runtime logs、health checks | Workers Logs、Analytics等 |
| 構成の感触 | 普通のサーバーに近い | エッジ／サーバーレスとして考える |

比較は「どちらが上か」ではなく、どの種類のアプリにどちらが自然かを理解するために行う。

## 19. 画面構成

初版は4画面相当でよい。

### タイトル

- タイトル
- 短い導入文
- 「本日の配達を始める」
- 「遊び方」
- 端末内自己ベスト

### ゲーム

- マップ
- 現在階
- 体力
- 荷物状態
- 所持アイテムまたは一時効果
- 簡潔な行動ログ
- PC用キー案内
- スマートフォン用方向ボタンと必要な行動ボタン

### 階間

- 到達階
- 配達スキル三択
- 次の階へ進む

### 結果

- 配達成功または失敗理由
- 到達階
- スコア内訳と総合スコア
- 端末内自己ベスト
- 再挑戦

## 20. UI・デザイン

### 視覚方針

- 深夜の業務用建物
- コンクリート、非常灯、荷物用エレベーター、階数表示
- 暗色を基調にしてよいが、文字とマップの判読性を最優先する
- 台車、ガムテープ、伝票、非常口などの日常物をゲーム記号にする
- ピクセルアート、ASCII風、単純な図形のいずれでも成立させる
- 外部画像素材がなくても完成する設計を優先する

### 操作性

- タップ対象はスマートフォンで押しやすい大きさにする
- 読み込み中と通信失敗を画面に明示する
- API障害時もゲーム本編は遊べるようにする
- 色だけで体力、危険、出口を区別しない
- 音声は任意。音を入れる場合はミュートを用意する

## 21. 一般公開を見据えた最低限の安全性

- ユーザー登録を作らない
- 個人情報を収集しない
- Cookieを必須にしない
- 書き込みAPIを作らない
- エラーレスポンスに秘密値、内部パス、スタックトレースを出さない
- 依存パッケージを増やしすぎない
- セキュリティヘッダーを適切に設定する
- 不要なアクセス解析を導入しない

## 22. テスト要件

### ゲームロジック

- 同一シードから同一結果が得られる
- 開始地点から出口へ到達可能
- マップ生成が規定回数以内で終わる
- 移動、攻撃、ダメージ、回復
- 体力または荷物状態が0になった際の終了
- 階層進行
- スコア計算

### API

- health checkが2xxを返す
- daily seedが同じ日本日付で一致する
- configが公開可能な値だけを返す
- 不正なパスや巨大リクエストに安全に応答する

### ビルド

クリーンな環境で次が動くこと。

- `npm test`
- `npm run build`
- `npm start`

ブラウザE2Eは余力があれば追加する。

## 23. 初版完成条件

- READMEの手順でローカル起動できる
- タイトルからB100成功または途中失敗まで通して遊べる
- 10区画の進行がある
- PCとスマートフォンの両方で操作できる
- 結果画面にスコアと内訳が出る
- 自己ベストを端末内へ保存できる
- 同じ日のデイリーシードが一致する
- Render Web Serviceとしてデプロイできる
- `onrender.com` URLを家族へ渡せる
- health checkが設定されている
- GitHub pushから自動デプロイできる
- テストと本番ビルドが通る
- READMEにローカル起動、Renderデプロイ、既知の制約が書かれている
- `docs/render-notes.md` に観察項目の雛形がある

## 24. 一日の実装順序

1. プロジェクト初期化、ローカルWeb Service、health check
2. シード付きマップ生成と移動
3. 敵、戦闘、体力、荷物状態
4. 10区画進行とB100の終了
5. タッチUI、タイトル、結果画面
6. スコアと端末内自己ベスト
7. デイリーAPIとフォールバック
8. `render.yaml` とRenderデプロイ
9. 自動テスト
10. 家族試遊を想定した難易度・操作修正
11. READMEとRender観察ノート

節目ごとに `npm test` と `npm run build` を通す。

## 25. 時間超過時に削る順序

上から順に削る。

1. 音、細かなアニメーション、装飾演出
2. 練習配達
3. 敵、アイテム、スキルの種類数
4. B100専用の複雑な障害
5. ブラウザE2Eテスト
6. 詳細な自己ベスト演出

削ってはいけないもの:

- Render Web Serviceとしてのデプロイ
- health check
- GitHubからの自動デプロイ
- 開始から終了までのゲームループ
- 10区画でB100へ到達する構造
- スマートフォン操作
- 結果画面のスコア
- 同一シードの決定性
- API障害時もゲーム本編が遊べること

## 26. 将来のPostgres導入

初版には含めない。

導入を再検討する条件:

- 家族以外にも継続して遊ばれる
- デイリーランキングを本当に置きたい
- 複数端末間で記録を共有したい
- 実績、プレイ履歴、運営データを永続保存したい
- 30日間のRender Postgres実験を行う具体的な目的ができた

導入時には、スコア送信API、入力検証、レート制限、不正対策、プライバシー表示、マイグレーション、バックアップ方針を別途設計する。現在の初版を、将来のDB導入のために複雑化しない。

## 27. 初版後の候補

- Render Postgresによるデイリーランキング
- 建物や受取人の追加シナリオ
- リプレイコードまたはシード共有
- 実績
- 複数の配達員
- 荷物の種類によるルール変化
- Cloudflare Workers版の試作
- 独自ドメイン
- PWA化

## 28. 実験後に答えたい問い

- Renderで普通のNode.jsサーバーを公開する体験はどうだったか
- GitHub pushから公開まで何が自動で行われたか
- health check、ログ、環境変数はどこで設定し、どう見えたか
- 無料Web Serviceの休止と復帰は家族向けゲームでどの程度気になったか
- 静的サイトだけをCloudflareへ置く場合と比べ、サーバーを持つ意味はあったか
- 同じゲームをCloudflare Workersへ移す場合、どこを考え直す必要があるか
- 次の小規模アプリではRenderとCloudflareのどちらを自然に選ぶか

このプロジェクトは、Renderを採用し続けること自体を成功条件にしない。ゲームを一本公開した経験から、両者の設計思想の違いを理解できた時点で、実験として成功とする。
