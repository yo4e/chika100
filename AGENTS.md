# Codexへの指示

`DESIGN.md`を基準に、初版を自律的に実装してください。

今日の最優先目標は、Render上のURLを家族へ渡し、Macとスマートフォンの双方から、タイトル画面→ゲーム開始→途中失敗またはB100到達→結果表示→再挑戦まで通して遊べる状態にすることです。

- 初版ではPostgresとランキングを実装しない
- スコアと自己ベストはブラウザ側で扱う
- Node.jsのRender Web Serviceとして、静的配信、`/api/daily`、`/api/config`、`/api/health`を実装する
- `render.yaml`、README、必要なテストを用意する
- 細部は合理的に判断し、ブロッキング要因がない限り逐一確認せず、実装・テスト・修正まで進める
- 時間超過時は`DESIGN.md`の削る順序に従い、遊べる完成品とRenderデプロイを優先する
